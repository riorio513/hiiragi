# うまくいかないときに見るところ

## 画面まわり

**ログイン画面に戻されてしまう**
Cookie（`hiiragi_session`）が消えています。30日で切れるほか、ブラウザの
シークレットウィンドウやサイトデータの削除でも消えます。もう一度ログインしてください。

**「メールアドレスかパスワードが違います」から進めない**
5回続けて失敗すると15分ロックされます。時間をおいてください。
パスワードを忘れた場合、いまは管理者がデータベースで作り直す必要があります
（`hiiragi.users` の該当行を削除して、もう一度アカウントを作るのがいちばん簡単です）。

**登録や保存を押しても固まって見える**
`npm run dev` で動かしていると、OneDrive 配下では画面ごとの初回表示に
10〜60秒かかります。**`npm run serve`（本番ビルド）を使ってください。**

## 監視まわり

**「状態不明」ばかりになる**
そのサイトのHTMLに、在庫を表す手がかり（JSON-LD・metaタグ・ボタンの文言）が
見つかっていません。**これは仕様です。** 推測で「在庫なし」と表示しません。
対応するには `src/server/monitor/adapters.ts` に、そのサイト用の言葉を足します。

```ts
{
  name: "example",
  label: "〇〇ショップ",
  hosts: /(^|\.)example\.jp$/i,
  minIntervalSec: 900,
  positive: [...COMMON_POSITIVE, "ご購入はこちら"],
  negative: [...COMMON_NEGATIVE, "ただいま品切れ中"],
  supported: true,
}
```

**「取得エラー」が続く**

| 表示 | 意味 | できること |
| --- | --- | --- |
| 商品ページが見つかりません | 404/410。ページが消えたかURLが変わった | 販売ページを開いて、新しいURLで登録し直す |
| 販売サイトからアクセスを断られました | 403/401 | そのサイトは自動確認を拒んでいます。監視を止め、公式アカウントの登録に切り替える |
| アクセスが多すぎると判断されました | 429 | 自動で間隔が空きます。しばらく待つ |
| 応答がありませんでした | 15秒たっても返ってこない | 一時的なことが多い。続くようなら監視を止める |
| robots.txt でこのページの取得が禁止されています | サイトの意思表示 | 監視できません。販売開始予定を登録して通知を使ってください |

エラーが3回続くと通知が届き、間隔は最大6時間まで自動的に広がります。
管理画面の「監視対象」から手動で再開できます。

**登録しようとしたら「監視できません」と断られる**
Amazon・メルカリは利用規約で自動巡回が禁止されているため、登録できません。
商品名と販売開始予定だけ登録して、時刻が近づいたときの通知を使ってください。

**通知が来ない**
- 商品の「通知」がOFFになっていないか（商品詳細の下）
- 「監視をお休みする時間帯」に入っていないか（設定）
- 同じ出来事は1回しか通知しません。在庫ありのままでは再通知されません
  （いったん在庫なしに戻って、また在庫ありになると通知します）
- 価格通知は、条件を満たし続けている間は繰り返しません

**確認の間隔が思ったより長い**
定期実行は GitHub Actions の5分ごとで、GitHub側の混雑で数分ずれます。
販売開始のその瞬間だけは、商品詳細の「いま確認する」を押してください。

## 開発まわり

**`npm` が見つからない**
このPCでは PATH に入っていません。`"/c/Program Files/nodejs/npm.cmd"` のようにフルパスで呼びます。

**`npm run migrate` が失敗する**
`.env.local` の `DATABASE_URL` を確認してください。Supabase のパスワードに記号が入っていると、
URLエンコードが必要な場合があります。

**ビルドで `Cannot apply unknown utility class` と出る**
Tailwind v4 では、`@layer components` の中で自作クラスを `@apply` できません。
`globals.css` のように、共通部分はセレクタを並べて書いてください。

**`Cannot call impure function during render` と怒られる**
React の新しいルールで、描画中に `Date.now()` を呼べません。
現在時刻が要るときは、サーバー側（`select now()`）から受け取って props で渡してください。

**サーバー起動中にビルドし直したのに変わらない**
`npm run serve` は「ビルドしてから起動」です。起動したままビルドしても反映されません。
いったん止めて `npm run serve` をやり直してください。
