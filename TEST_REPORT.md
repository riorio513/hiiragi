# 動作確認の結果（2026-09-09）

確認した場所：ローカル本番ビルド（`npm run serve` / http://localhost:3600）
データベース：Supabase Postgres の `hiiragi` スキーマ（本番と同じもの）

## 1. 自動テスト

```
npm test    → 4ファイル 30件 すべて成功
npm run lint → 指摘なし
npm run build → 成功（TypeScript の型チェックを含む）
```

| ファイル | 何を固定したか |
| --- | --- |
| `tests/url.test.ts` | スキーム補完、計測パラメータ・末尾スラッシュ・ハッシュを落として同じURLに揃える、壊れたURL・`javascript:`・`file:` を断る、社内アドレス（127.x / 10.x / 172.16-31.x / 192.168.x）を断る |
| `tests/analyze.test.ts` | JSON-LD > meta > ボタン文言 > 本文 の優先順位、おすすめ欄の「SOLD OUT」に引っかからない、買える言葉と買えない言葉が両方あるときは状態不明、手がかりが無いときも状態不明、`<script>` の中身は本文として数えない、壊れたJSON-LDでも落ちない |
| `tests/robots.test.ts` | 全面禁止、最長一致（禁止の中の一部だけ許可）、自分あてのグループ優先、空の `Disallow:` は全許可、`*` と `$`、コメント行 |
| `tests/schedule.test.ts` | 通常30分、販売開始が近いほど短く（10分→5分→1分→30秒）、サイトの最短間隔で下限を切る、お休み時間は3時間、お休み中でも販売開始予定を優先、エラー時のバックオフ（上限6時間）、日をまたぐお休み時間 |

## 2. ブラウザでの通し確認

指示書35章の項目を、実際にブラウザを操作して確認しました。

| # | 確認したこと | 結果 |
| --- | --- | --- |
| 1 | トップページが表示される | ✅ 「できること／できないこと」まで表示 |
| 2 | 新規登録画面が表示される | ✅ |
| 3 | アカウント作成・ログインできる | ✅ 最初の1人が自動的に管理者になった |
| 4 | ダッシュボードが表示される | ✅ 初回は3ステップの案内つき |
| 5 | 商品を登録できる | ✅ 名前だけでも、URL付きでも |
| 6 | 店舗を登録できる | ✅ 手動追加と、URLのドメインからの自動作成の両方 |
| 7 | 商品URLを登録できる | ✅ 貼り付け→検証→アダプター判定→初回取得まで自動 |
| 8 | 初回監視が実行される | ✅ 登録直後に1回取得し、その場で結果を表示 |
| 9 | 在庫状態が表示される | ✅ 🟢在庫あり / 🔴在庫なし / ⚪状態不明 / ⚠️取得エラー |
| 10 | 価格が表示される | ✅ JSON-LD から 2,980円 を取得 |
| 11 | 通知が表示される | ✅ 在庫復活・希望価格の2種類を確認 |
| 12 | 通知から商品ページへ移動できる | ✅ 別タブで開く（`rel="noopener noreferrer nofollow"`） |
| 13 | 商品詳細画面が表示される | ✅ 在庫・価格・希望価格・販売店・URL・監視状態・履歴・通知・エラー |
| 14 | 監視履歴が表示される | ✅ 日時・状態・価格・HTTPステータス・判定の根拠 |
| 15 | ログアウトできる | ✅ Cookieが消え、保護された画面はログインへ戻る |
| 16 | 別ユーザーから他人のデータが見えない | ✅ 下の「3.」参照 |
| 17 | スマートフォン表示が崩れない | ✅ 375×812 で確認。下部タブ・カード・表（横スクロール）すべて収まる |
| 18 | エラー状態が適切に表示される | ✅ 下の「4.」参照 |

## 3. データの分離

- 利用者A（管理者）が商品を1件登録
- 利用者Bで新規登録 → ダッシュボードは空。ヘッダーに「管理」リンクは出ない
- 利用者Bが、AのURL `/products/<AのID>` を直接開く → **404**（内容は一切返らない）
- ログアウト状態で同じURLを開く → ログイン画面へリダイレクト

## 4. エラーの扱い

`https://example.com/product/1?utm_source=test` を登録して確認：

- 計測パラメータが落ちて `https://example.com/product/1` として保存された
- HTTP 404 → **「在庫なし」ではなく「取得エラー」**として記録
- 商品詳細に「確認できない状態が1回続いています／商品ページが見つかりません（削除・URL変更の可能性）／
  最後に成功したのは —／できること：…／一時停止ボタン」を表示
- 次回確認が 30分後 → **1時間後**に自動で後ろ倒し（エラー時のバックオフ）

`https://www.amazon.co.jp/dp/...` を登録しようとした場合：

> Amazonは利用規約で自動巡回を禁止しているため、このアプリでは監視できません。
> 販売開始予定日時を登録して、通知でお知らせする使い方をおすすめします。

と表示して登録を断りました。

## 5. 通知の重複防止

同じ監視対象を3回続けて確認しても、通知は増えませんでした。

| 操作 | 結果 |
| --- | --- |
| 初回取得（在庫なし・2,980円、希望3,000円） | 希望価格の通知 1件 |
| 在庫ありに切り替えて確認 | 在庫復活の通知 1件（`stock:<対象ID>:1`） |
| そのまま2回追加で確認 | **通知は増えない**（価格条件も継続中のため再通知なし） |
| 在庫なしに戻して、また在庫ありに | 在庫復活の通知が新しい鍵（`:2`）で1件 |

データベースの確認結果（3回チェック後）：

```
notifications: back_in_stock=1, price_drop=1
monitor_results: in_stock=2, out_of_stock=1
monitor_targets: restock_seq=1, consecutive_errors=0
```

## 6. 複数人での共有監視

利用者Aと利用者Bが**同じ商品ページURL**（Bは `?utm_source=line` 付き）を登録：

- URLが正規化されて同じ監視対象1件にまとまった（管理画面に「見ている人 2」と表示）
- 在庫を切り替えて1回だけ外部アクセスしたところ、**A・B両方に通知が届いた**
- それぞれ自分がつけた商品名で届いた
  （A：「【デモ】サンプル商品A（練習用） の在庫が復活しました」／
  B：「Mellojoy ぬいぐるみ（テスト） の在庫が復活しました」）
- Bの希望価格は2,500円だったため、価格通知はBには届かなかった（条件を満たしていない）

## 7. 定期監視の入口

```
POST /api/cron/check                      → 401 {"error":"unauthorized"}
POST /api/cron/check（合言葉あり）        → 200 {"ok":true,"checked":0,...}
   ↑ まだ次回確認時刻が来ていないので0件。サイトへ無駄なアクセスをしない
次回確認時刻を今にして再実行             → 200 {"checked":1,"targets":[{"status":"in_stock","price":2980}]}
```

管理画面の「監視の実行状況」に実行履歴（きっかけ・確認数・エラー数・通知数）が並ぶことも確認しました。

## 8. 確認できていないこと

- **実在の販売サイト（楽天・Yahoo!・BASE等）での在庫判定**。
  監視の仕組みと汎用ルールは確認済みですが、各サイトの実ページでの判定精度は、
  実際の商品URLを登録してみないと分かりません。最初の1件を登録したときの表示で確認してください。
- **GitHub Actions からの定期実行**。ワークフローは用意済みですが、
  デプロイ後にシークレットを登録してからでないと動きません（`SETUP.md` の5章）。
- 販売開始予定の接近通知（`sale_soon`）は、時刻を跨ぐ必要があるためコードとテスト
  （`schedule.test.ts`）での確認にとどめています。
