# りおさんにお願いする操作

公開・デプロイ・定期監視の設定はすべて終わっています。
**残っているのは、下の「1. 最初のアカウントを作る」だけです。**

---

## 1. 最初のアカウントを作る（所要2分）

**開く画面**：https://hiiragi-topaz.vercel.app/signup

動作確認に使ったアカウントは消してあるので、いまデータベースは空っぽです。
**最初にアカウントを作った人が、自動的に管理者になります。** りおさんが最初に作ってください。

**入力するもの**

| 欄 | 入れるもの |
| --- | --- |
| 呼び名（任意） | 画面右上に出るだけの名前。空欄でも大丈夫です |
| メールアドレス | いつも使っているもので構いません。確認メールは届きません |
| パスワード | **8文字以上・英字と数字の両方**。りおさんが決めたものを、私には教えないでください |

**押すボタン**：「アカウントを作る」

**そのあと起きること**：ホーム画面（ダッシュボード）に移り、右上に「管理」リンクが出ます。
これが出ていれば管理者になれています。

> パスワードはこのチャットに貼らないでください。
> このアプリはパスワードをそのままの形では保存せず、元に戻せない形に変換して保存します。

---

## 2. 使い方を試す（任意・所要2分）

1. 画面下の「⚙️ 設定」→「デモデータを用意する」を押す
2. 商品の画面に移るので、「監視している商品ページ」の**「ページを開く」**を押す
3. 練習用の模擬ショップが開くので、**「在庫ありにする（通知が届きます）」**を押す
4. 「通知を見る」を押すと、「在庫が復活しました」が届いています

この模擬ショップはこのアプリの中にある練習用ページで、実在の販売サイトには一切アクセスしません。

---

## 3. 本番の商品を1つ登録する（任意）

1. 画面下の「＋ 商品を追加」
2. 商品名（例：`Mellojoy 〇〇`）を入れる
3. 販売ページをブラウザで開き、アドレス欄（`https://…`）をコピーして「商品ページのURL」に貼る
4. 希望価格を入れる（この金額以下になったら通知します。空欄でも可）
5. 「登録して監視をはじめる」

貼り付けた時点で1回目の確認が走り、在庫と価格がその場で表示されます。
「状態不明」になった場合は、そのサイトの書き方に合わせたルールを足す必要があるので、
**URLを教えてもらえれば対応します**（在庫なしと決めつけない設計になっています）。

---

## 4. 友人に渡す（任意）

渡すものは URL ひとつだけです。

```
https://hiiragi-topaz.vercel.app
```

友人はそのURLを開いて「はじめる（アカウント作成）」を押すだけで使えます
（招待コードは不要にしてあります）。友人向けの説明文は README.md の冒頭にそのまま使える形で書いてあります。

---

## 完了済み（りおさんの操作は不要でした）

- [x] GitHub の公開リポジトリ作成とプッシュ（https://github.com/riorio513/hiiragi）
- [x] Vercel プロジェクト作成・環境変数の登録・本番デプロイ
- [x] Vercel の Deployment Protection（SSO）解除 ＝ 友人のスマホからも開ける状態
- [x] データベース（Supabase の `hiiragi` スキーマ）のテーブル作成
- [x] GitHub Actions のシークレット登録（`APP_URL` / `CRON_SECRET`）
- [x] 5分ごとの定期監視ワークフローの登録と、手動実行での疎通確認（HTTP 200）
- [x] 動作確認に使ったアカウント・デモデータの削除

## 費用について

**Claude Pro 以外の支払いは発生しません。** クレジットカードの登録も不要です。

| サービス | プラン | 上限に達したら |
| --- | --- | --- |
| Vercel | Hobby（無料） | 停止するだけ。自動で有料になりません |
| Supabase | Free（500MB） | 停止・読み取り専用になるだけ |
| GitHub Actions | 公開リポジトリのため無料・無制限 | — |

詳しい確認結果は [TECHNICAL_DECISIONS.md](TECHNICAL_DECISIONS.md) にあります。
