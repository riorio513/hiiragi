# 技術構成と、その理由

前提：**Claude Pro の料金以外、1円もかけない。** AI（OpenAI / Claude / Gemini 等のAPI）は使わない。
判定はすべてルールベース。

## 採用した構成

| 役割 | 選んだもの | 無料枠 | クレジットカード登録 | 超過時の自動課金 |
| --- | --- | --- | --- | --- |
| 画面・サーバー | Next.js 16（App Router）+ TypeScript + Tailwind v4 | — | — | — |
| ホスティング | Vercel Hobby | 個人利用は無料 | 不要 | **なし**（上限に達すると停止。Hobby は課金に進めない） |
| データベース | Supabase Postgres（既存プロジェクトに相乗り、`hiiragi` スキーマ） | Free: 500MB | 不要 | なし（上限で読み取り専用/一時停止） |
| 定期実行 | GitHub Actions（`schedule`、5分ごと） | **公開リポジトリは無制限で無料** | 不要 | なし |
| ログイン | 自前（Node標準 scrypt + jose の署名付きCookie） | — | — | — |
| 通知 | アプリ内通知のみ（メール・LINE・プッシュは使わない） | — | — | — |

**すべてクレジットカード登録なしで動きます。**

## なぜこの組み合わせか

### 定期実行を Vercel Cron ではなく GitHub Actions にした
Vercel Hobby の Cron は **1日1回まで**。在庫監視には足りない。
GitHub Actions の `schedule` は5分間隔まで指定でき、**公開リポジトリなら実行時間は無料・無制限**。

- 秘密の合言葉（`CRON_SECRET`）を付けて `POST /api/cron/check` を叩くだけの、10秒で終わる仕事
- GitHub 側の混雑で数分ずれることがある（GitHub の仕様。遅延前提で設計してある）
- 販売開始直前に30秒間隔が必要な場面では、**5分に1回の呼び出しで足りない**。
  そのため商品詳細に「いま確認する」ボタンを置き、
  販売開始のタイミングだけは利用者が手で押せるようにしてある

> **リポジトリは公開（public）にする必要があります。**
> 非公開リポジトリだと Actions の無料枠は月2,000分で、しかも1回の実行が1分に切り上げられるため、
> 5分ごと（月8,640回）では確実に足りず、超過分は有料になります。
> ソースコードは公開しても、秘密の値（DB接続文字列・合言葉）はリポジトリに入れません
> （GitHub Secrets と Vercel の環境変数に置きます）。

### データベースを新規 Supabase プロジェクトではなく既存プロジェクトへ相乗りさせた
Supabase Free は**組織あたり2プロジェクトまで**で、既に埋まっている。
テーブル名の衝突を避けるため、専用の **`hiiragi` スキーマ**に閉じ込めた
（他アプリのテーブルには一切触れない）。

### Prisma ではなく `pg` を直接使う
既存の他アプリ（ローズマリー／オリーブ）と同じやり方。ビルドが軽く、
`npm` のインストールスクリプト許可（Prisma のエンジン取得）でつまずかない。

### 認証を Supabase Auth ではなく自前にした
- メール確認を必須にすると、友人に配るときの手間が増える（要件7-2）
- 送信メールの無料枠（Supabase の内蔵SMTPは1時間あたり数通）に縛られたくない
- パスワードは Node 標準の `scrypt` でハッシュ化して保存（平文保存なし）
- ログイン失敗5回で15分ロック、Cookie は `httpOnly` + `SameSite=Lax` + 本番のみ `Secure`

`pg` で直接つなぐため Supabase の RLS は効きません。代わりに
**すべてのSQLに `user_id` の条件を必ず入れる**方式でデータを分離しています
（`TEST_REPORT.md` に他人のデータが見えないことの確認結果があります）。

### AIを使わない在庫判定
次の順で見て、判断できなければ**必ず「状態不明」**にします（在庫なしと決めつけない）。

1. `<script type="application/ld+json">` の schema.org `availability` / `price`
   （サイトが自分で書いている情報なので、いちばん確か）
2. `<meta property="product:availability">` などの meta タグ
3. **ボタンやリンクの文言**（「カートに入れる」「SOLD OUT」など）
   → 押せる場所だけを見るので、おすすめ欄の「SOLD OUT」に引っかかりにくい
4. ページ本文の文言（買える言葉と買えない言葉の片方しか無いときだけ採用）

新しい販売サイトへ対応するときは `src/server/monitor/adapters.ts` に1件足すだけです。

### 監視対象を利用者ごとに持たず、URL単位で共有する
`monitor_targets` は **URL単位で1行**。複数の利用者が同じ商品ページを登録しても、
外部サイトへのアクセスは1回で済み、結果は全員に配られます（要件31）。
通知の重複防止は「そのURLが何回目の在庫復活か」という連番（`restock_seq`）を鍵にしているので、
2人が同時に見ていても、1人あたり1通だけ届きます。

### 外部サイトへの配慮
- `robots.txt` を取得して、禁止されているページは監視しない（週1回再確認）
- 販売サイトごとに最短アクセス間隔を持つ（楽天・Yahoo!は15分、汎用は30分）
- Amazon・メルカリは利用規約で自動巡回が禁止されているため、**登録段階で断る**
- 取得は15秒でタイムアウト、3MBを超えるページは読まない
- User-Agent に `HiiragiWatch` と名乗る（ブラウザのふりをしてすり抜けない）

### 使わなかったもの
| 検討したもの | 使わなかった理由 |
| --- | --- |
| Vercel Cron | Hobby は1日1回まで |
| Cloudflare Workers Cron | 無料で使えるが、DBとアプリをVercelに置く構成と二重管理になる。将来の逃げ道として残す |
| メール通知（Resend等） | 無料枠が小さく、超過時の課金リスクがある。まずアプリ内通知だけにする |
| Web Push | iOS では「ホーム画面に追加」が必要で、友人に説明する手間が増える。次の段階で検討 |
| Playwright での監視 | 実行時間が長く、無料枠を食う。JavaScript が必要なサイトは今回対象外 |
| 販売場所の自動検索 | 無料の検索APIが実質ない。「商品発見」は別モジュールとして後回し（要件24） |

## 将来の拡張ポイント

- **新しい販売サイト対応**：`adapters.ts` に1件追加
- **販売場所の自動探索**：`src/server/targets.ts` が「発見」と「監視」の境目。
  発見側だけを差し替えれば、URL登録の代わりに候補提示ができる
- **SNSの販売告知の追跡**：`official_accounts` テーブルは既にある。
  取得処理を足すなら `monitor_targets` と同じ形（対象・間隔・結果）で作ると通知に乗る
- **通知経路の追加**：`notifications` テーブルに行が入る場所（`runner.ts` の `insertNotification`）が
  1か所なので、そこから外部送信を足せる
